import SignInForm from '@/components/form/SignInForm';
import { FC } from 'react';

const page: FC = () => {
  return (
    <div className="w-full">
      <SignInForm />
    </div>
  );
};

export default page;
